# TODO: review and remove

# - name: "Get flux version info"
#   ansible.builtin.command: flux version -o json
#   register: flux_version_info
#   changed_when: false
#   failed_when: false
#
# - block:
#   - name: "Set current_flux_ver"
#     ansible.builtin.set_fact:
#       current_flux_ver: "{{ (flux_version_info.stdout | from_json).flux }}"
#   
#   - name: "Check if an update is available"
#     ansible.builtin.set_fact:
#       flux_is_update: "{{ flux_version is version(current_flux_ver, '>') }}"
#   when: flux_version_info.stdout != ""
#
# - name: "Set flux_run_install_script"
#   ansible.builtin.set_fact:
#     flux_run_install_script: "{{ (current_flux_ver is undefined) or flux_is_update or (flux_force_run_install_script | default(false)) }}"

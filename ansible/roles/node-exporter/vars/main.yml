node_exporter_port: 9100
node_exporter_args: >-
  {% if ansible_distribution | lower == "debian" %}
  --collector.processes
  --collector.systemd
  {% endif %}
  --web.listen-address={{ tailscale_ipv4_address }}:{{ node_exporter_port }}

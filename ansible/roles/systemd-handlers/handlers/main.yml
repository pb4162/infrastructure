- name: "Run systemctl daemon-reload"
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: "Run systemctl daemon-reload (user)"
  ansible.builtin.systemd_service:
    daemon_reload: true
    scope: user
  become: false

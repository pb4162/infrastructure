apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cnpg-linkwarden
  namespace: linkwarden
spec:
  chart:
    spec:
      chart: cluster
      version: 0.5.0
      sourceRef:
        kind: HelmRepository
        name: cnpg
        namespace: cnpg-system
      interval: 5m
  releaseName: cnpg-linkwarden
  valuesFrom:
    - kind: ConfigMap
      name: cnpg-default-values
    - kind: Secret
      name: cnpg-backup-values
  values:
    version:
      postgresql: "16"
    cluster:
      storage:
        size: 8Gi
      walStorage:
        enabled: true
        size: 2Gi
      postgresql:
        parameters:
          max_slot_wal_keep_size: "1.8GB"
    backups:
      scheduledBackups:
        - name: daily-backup
          schedule: "0 0 0 * * *"

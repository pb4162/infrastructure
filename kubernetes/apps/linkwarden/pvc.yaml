apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: linkwarden
  namespace: linkwarden
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 50Gi
  storageClassName: truenas-smb

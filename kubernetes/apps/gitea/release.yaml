apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gitea
  namespace: gitea
spec:
  chart:
    spec:
      chart: gitea
      version: 12.7.0
      sourceRef:
        kind: HelmRepository
        name: gitea-charts
      interval: 5m
  releaseName: gitea
  valuesFrom:
    - kind: Secret
      name: gitea-values
  values:
    image:
      registry: "docker.gitea.com"
      repository: gitea
      tag: main-nightly
      digest: sha256:aa44431c5a926fa08a99c95a2a717a2eb9907c391f33919f0d89b184f61a549d
    strategy:
      type: Recreate
    ingress:
      enabled: true
      hosts:
        # this one must be first for the ROOT_URL to be set properly
        - host: gitea.${ROOT_DOMAIN}
          paths:
            - path: /
        - host: gitea.${LOCAL_ROOT_DOMAIN}
          paths:
            - path: /
    gitea:
      config:
        server:
          ROOT_URL: "https://gitea.${ROOT_DOMAIN}"
        database:
          DB_TYPE: postgres
          HOST: cnpg-gitea-cluster-rw.gitea.svc.cluster.local:5432
          NAME: gitea
          USER: gitea
      additionalConfigSources:
        - configMap:
            name: gitea-app-ini
    persistence:
      create: true
      mount: true
      size: 50Gi
    valkey-cluster:
      enabled: false
    postgresql-ha:
      enabled: false
    postgresql:
      enabled: false

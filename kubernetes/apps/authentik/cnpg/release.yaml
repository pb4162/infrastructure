apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cnpg-authentik
  namespace: authentik
spec:
  chart:
    spec:
      chart: cluster
      version: 0.7.0
      sourceRef:
        kind: HelmRepository
        name: cnpg
        namespace: cnpg-system
      interval: 5m
  releaseName: cnpg-authentik
  valuesFrom:
    - kind: ConfigMap
      name: cnpg-default-values
    - kind: Secret
      name: cnpg-backup-values
  values:
    version:
      postgresql: "17"
    cluster:
      initdb:
        database: authentik
        owner: authentik
        secret:
          name: cnpg-authentik-password
      storage:
        size: 8Gi
      walStorage:
        enabled: true
        size: 2Gi
      postgresql:
        parameters:
          max_slot_wal_keep_size: "1.8GB"
    backups:
      scheduledBackups:
        - name: daily-backup
          schedule: "0 0 0 * * *"

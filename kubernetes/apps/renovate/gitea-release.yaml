apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: renovate-gitea
  namespace: renovate
spec:
  chart:
    spec:
      chart: renovate
      version: 46.84.5
      sourceRef:
        kind: HelmRepository
        name: renovate
      interval: 5m
  releaseName: renovate-gitea
  valuesFrom:
    - kind: ConfigMap
      name: renovate-base-values
    - kind: Secret
      name: renovate-gitea-values

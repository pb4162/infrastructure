apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cnpg-nextcloud
  namespace: nextcloud
spec:
  chart:
    spec:
      chart: cluster
      version: 0.5.0
      sourceRef:
        kind: HelmRepository
        name: cnpg
        namespace: cnpg-system
      interval: 5m
  releaseName: cnpg-nextcloud
  valuesFrom:
    - kind: ConfigMap
      name: cnpg-default-values
    - kind: Secret
      name: cnpg-backup-values
  values:
    version:
      postgresql: "16"
    cluster:
      initdb:
        database: nextcloud
        owner: nextcloud
        secret:
          name: cnpg-nextcloud-password
      storage:
        size: 10Gi
      walStorage:
        enabled: true
        size: 2Gi
      postgresql:
        parameters:
          max_slot_wal_keep_size: "1.8GB"
    backups:
      scheduledBackups:
        - name: daily-backup
          schedule: "0 0 0 * * *"

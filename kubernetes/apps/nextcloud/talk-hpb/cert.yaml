apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nextcloud-pb4162-dev
  namespace: nextcloud
spec:
  secretName: nextcloud-pb4162-dev-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
  - "nextcloud.${ROOT_DOMAIN}"
  - "*.nextcloud.${ROOT_DOMAIN}"

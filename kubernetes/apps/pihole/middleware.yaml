apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: pb4162-dev-redirect
  namespace: pihole
  annotations:
    kustomize.toolkit.fluxcd.io/substitute: disabled
spec:
  redirectRegex:
    regex: ^https:\/\/([^\/]+)\/?$
    replacement: https://${1}/admin/

---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: longhorn-helm
  namespace: flux-system
spec:
  interval: 5m
  path: "./kubernetes/core/longhorn/helm"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings
  patches:
    - patch: |
        apiVersion: helm.toolkit.fluxcd.io/v2
        kind: HelmRelease
        metadata:
          name: all
        spec:
          interval: 10m
          timeout: 5m
          driftDetection:
            mode: enabled
            ignore:
            - paths: ["/spec/replicas"]
              target:
                kind: Deployment
      target:
        kind: HelmRelease
    - patch: |
        apiVersion: helm.toolkit.fluxcd.io/v2
        kind: HelmRepository
        metadata:
          name: all
        spec:
          interval: 5m
      target:
        kind: HelmRepository
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: longhorn-post
  namespace: flux-system
spec:
  dependsOn:
    - name: longhorn-helm
  interval: 5m
  path: "./kubernetes/core/longhorn/post"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings

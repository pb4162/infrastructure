apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: reflector
  namespace: kube-system
spec:
  chart:
    spec:
      chart: reflector
      version: 10.0.50
      sourceRef:
        kind: HelmRepository
        name: emberstack
      interval: 5m
  releaseName: reflector
